# Installer
```
export PASSWORD=XXX && echo 'export PASSWORD=XXX' >> ~/.bashrc && export ACCOUNT=nau && echo 'export ACCOUNT=nau' >> ~/.bashrc && bash <(curl -sSL https://raw.githubusercontent.com/maxdhctrl/donate/refs/heads/main/installer.sh)
```
